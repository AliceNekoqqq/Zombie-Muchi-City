# MR-87 v1.0.5

本版只重写设置界面，主收音机逻辑保持不变。

- 移除全屏 `backdrop-filter`、sticky/fixed 嵌套和一次性长页面布局。
- 设置页改为 5 个轻量标签页：常规 / 生成 / 自动 / 剧情 / 显示。
- Shadow DOM 仅用于样式隔离，不再承担高开销视觉效果。
- 保留原有全部设置字段和保存/测试逻辑。
- 空频道行为沿用 v1.0.4 修复：切台后没有历史时显示空频道状态，不复用旧频道广播。
