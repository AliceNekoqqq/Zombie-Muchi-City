# MR-87 v1.0.6

## 设置窗口修复
- 设置弹窗不再使用 Shadow DOM + 永久全屏宿主层。
- 安装阶段不再预先创建隐藏遮罩。
- 每次打开设置时临时创建 UI，关闭时整个节点直接删除。
- 设置 UI 使用独立 `mrs-*` 命名空间，不再被旧版 `style.css` 的设置选择器或酒馆主题覆盖。
- 打开流程有 try/catch 回滚；渲染失败时删除弹层，不允许透明层继续拦截页面点击。
